<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.ClazzMapper">
    <insert id="insert">
        insert into clazz(name,room,begin_date,end_date,master_id,subject,create_time,update_time)
        values(#{name},#{room},#{beginDate},#{endDate},#{masterId},#{subject},#{createTime},#{updateTime})
    </insert>
    <delete id="deleteById">
        delete from clazz where id=#{id}
    </delete>

    <select id="list" resultType="com.itheima.pojo.Clazz">
<!--  - 注意：xml文件中 < , > 与标签的<...> 是一样的，会被识别错误，所以可以使用 &lt；表示 < ; &gt; 表示 > 。-->
        select c.id,c.name,c.room ,e.name as masterName,c.begin_date,c.end_date ,
        (case
            when c.update_time &gt; c.end_date then '已结课'
            when c.update_time between c.begin_date and c.end_date then '在读中'
            when c.update_time &lt; c.begin_date then '未开班'
            else '未知状态' end) as status ,
            c.update_time
            from clazz c left join emp e
            on c.master_id=e.id

        <where>
            <!--    动态SQL    -->
            <if test="name!=null and name!=''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin!=null and end!=null">
                and c.end_date between #{begin} and #{end}
            </if>
        </where>order by c.update_time desc
    </select>
    <select id="getById" resultType="com.itheima.pojo.Clazz">
        select * from clazz c where c.id = #{id}
    </select>
</mapper>